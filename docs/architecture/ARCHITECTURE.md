# ARCHITECTURE (superseded)

The first architecture draft (Vite + Framer Motion) was replaced by the Next.js App Router + GSAP
architecture described in **[../IMPLEMENTATION_PLAN.md](../IMPLEMENTATION_PLAN.md)**. The stand-in policy,
verification gates and `data-ref` contract from the draft are carried over there (§7, §10).

`text-budgets.json` in this folder (metrics-only text budgets of the original) is still current.
