'use client';

/**
 * SocialRow — 4 Phosphor social icons (24px), hover opacity .5 (CSS).
 * STATUS: typed stub; implemented in the implementation phase (docs/IMPLEMENTATION_PLAN.md).
 */
export function SocialRow(props: { tone?: 'dark' | 'light' }) {
  void props;
  return null;
}
