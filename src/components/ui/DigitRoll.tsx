'use client';

/**
 * DigitRoll — NumberFlow-style digit strips (first-class #8); animated by rollDigits.
 * STATUS: typed stub; implemented in the implementation phase (docs/IMPLEMENTATION_PLAN.md).
 */
export function DigitRoll(props: { value: number; prefix?: string }) {
  void props;
  return null;
}
