'use client';

/**
 * RatingWidget — Avatars (masked overlap) + score line + star.
 * STATUS: typed stub; implemented in the implementation phase (docs/IMPLEMENTATION_PLAN.md).
 */
export function RatingWidget(props: { tone?: 'dark' | 'light' }) {
  void props;
  return null;
}
