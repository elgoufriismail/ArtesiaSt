'use client';

/**
 * PillButton — Pill with sliding dot (Circle A/B); hover is CSS (--hover-pill-*). INTERACTIONS.md §1.
 * STATUS: typed stub; implemented in the implementation phase (docs/IMPLEMENTATION_PLAN.md).
 */
export function PillButton(props: { label: string; href?: string; variant?: 'green' | 'white'; as?: 'a' | 'button' }) {
  void props;
  return null;
}
