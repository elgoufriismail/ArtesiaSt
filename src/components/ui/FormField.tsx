'use client';

/**
 * FormField — Framer-form-like field styles (VISUAL.md §4).
 * STATUS: typed stub; implemented in the implementation phase (docs/IMPLEMENTATION_PLAN.md).
 */
export function FormField(props: { kind: 'text' | 'email' | 'tel' | 'select' | 'textarea' | 'checkbox'; name: string; label: string; required?: boolean; options?: string[] }) {
  void props;
  return null;
}
