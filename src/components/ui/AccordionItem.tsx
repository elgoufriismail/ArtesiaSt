'use client';

/**
 * AccordionItem — FAQ item (first-class #12); animated by accordionToggle.
 * STATUS: typed stub; implemented in the implementation phase (docs/IMPLEMENTATION_PLAN.md).
 */
export function AccordionItem(props: { question: string; answer: string; defaultOpen?: boolean }) {
  void props;
  return null;
}
