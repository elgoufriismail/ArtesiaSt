'use client';

/**
 * Switch — Track + knob; animated by balanceSequence / pricingSwitch.
 * STATUS: typed stub; implemented in the implementation phase (docs/IMPLEMENTATION_PLAN.md).
 */
export function Switch(props: { on: boolean; label?: string; onToggle?: () => void; size?: 'balance' | 'pricing' }) {
  void props;
  return null;
}
