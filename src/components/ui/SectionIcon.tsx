'use client';

/**
 * SectionIcon — 64px masked icon + eyebrow (Phosphor glyph as mask).
 * STATUS: typed stub; implemented in the implementation phase (docs/IMPLEMENTATION_PLAN.md).
 */
export function SectionIcon(props: { icon: 'philosophy' | 'pricing' | 'journal'; label: string }) {
  void props;
  return null;
}
