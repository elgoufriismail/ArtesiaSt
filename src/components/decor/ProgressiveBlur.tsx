'use client';

/**
 * ProgressiveBlur — Fixed 220px, 8 backdrop-filter layers 0.156→20px (desktop only).
 * STATUS: typed stub; implemented in the implementation phase (docs/IMPLEMENTATION_PLAN.md).
 */
export function ProgressiveBlur(props: Record<string, never>) {
  void props;
  return null;
}
