'use client';

/**
 * DrawnPath — Original stand-in line art in the original viewBoxes; animated by drawPath (first-class #13).
 * STATUS: typed stub; implemented in the implementation phase (docs/IMPLEMENTATION_PLAN.md).
 */
export function DrawnPath(props: { name: 'heroLine1' | 'heroLine2' | 'howItWorks' | 'quoteLine' | 'quoteLineFaint' | 'pricingScribble' }) {
  void props;
  return null;
}
