'use client';

/**
 * WavesBackground — Fixed desktop layer with 2 wavy lines; opacity by wavesBackground (B5).
 * STATUS: typed stub; implemented in the implementation phase (docs/IMPLEMENTATION_PLAN.md).
 */
export function WavesBackground(props: Record<string, never>) {
  void props;
  return null;
}
