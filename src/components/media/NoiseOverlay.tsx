'use client';

/**
 * NoiseOverlay — Repeating grain texture, mix-blend-mode overlay.
 * STATUS: typed stub; implemented in the implementation phase (docs/IMPLEMENTATION_PLAN.md).
 */
export function NoiseOverlay(props: { tile: 'a' | 'b'; opacity?: number }) {
  void props;
  return null;
}
