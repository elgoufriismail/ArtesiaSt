'use client';

/**
 * ParallaxImage — Frame + overscanned image + noise layer; animated by imageParallax (first-class #4).
 * STATUS: typed stub; implemented in the implementation phase (docs/IMPLEMENTATION_PLAN.md).
 */
export function ParallaxImage(props: { slot: import('@/content/assets').StandInSlot; intensity: 'services' | 'storyLarge' | 'storySmall' | 'quote' }) {
  void props;
  return null;
}
