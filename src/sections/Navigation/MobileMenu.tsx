'use client';

/**
 * MobileMenu — Tablet/phone overlay (first-class #11); mobileMenuTimeline + useScrollLock.
 * STATUS: typed stub; implemented in the implementation phase (docs/IMPLEMENTATION_PLAN.md).
 */
export function MobileMenu(props: { open: boolean; onClose: () => void }) {
  void props;
  return null;
}
